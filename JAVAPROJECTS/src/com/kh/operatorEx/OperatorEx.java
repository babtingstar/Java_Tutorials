package com.kh.operatorEx;

public class OperatorEx {
    /*
    산술 연산자(Arithmetic Operators)

    + 덧셈
    - 뺄셈
    * 곱셈
    / 몫
    % 나머지

    할당 연산자 (Assignment Operators)

    = 대입
    += 덧셈 후 대입
    -= 뺄셈 후 대입
    *= 곱셈 후 대입
    /= 몫 연산 후 대입
    %= 나머지 연산 후 대입

    비교 연산자 (Relational Operators)

    == 값이 같으면 참
    != 값이 다르면 참
    > 좌항이 크면 참
    < 우항이 크면 참
    >= 좌항이 크거나 같으면 참
    <= 우항이 크거나 같으면 참

    논리 연산자 (Logical Operators)
    && AND 모두 참이면 참
    || OR 하나라도 참이면 참
    ! NOT 값을 반전함

    단항 연산자(Unary Operators)
    + 양수
    - 음수
    ++ 1증가(전위 / 후위)
    -- 1감소(전위 / 후위)
    ! 논리 부정 : !true => false

    *** 전위 연산자 (++var / --var)
        : 연산을 먼저 실행, 이후 연산이 완료된 값을 사용
    *** 후위 연산자 (var++ / var--)
        : 변수 값을 사용 후, 연산을 진행

    var++
    ++var
    int result = ++var;
    위 셋은 결과가 같음

    int result = var++;
    result 에 +1 하기 전의 var 값을 입력하고, 그 이후에 var 에 +1
    result+1 == var



    삼항 연산자(Ternary Operators)
    (   )? true : false
        조건이 참이면 첫 번째 값을 반환, 거짓이면 두 번째 값을 반환

    기타 연산자
    .   멤버 접근 연산자 ex.method1() : ex 내부에 있는 method1() 기능 호출
    []  배열 요소 접근 arr[0] : arr 배열의 첫 번째 요소에 접근
    ()  메서드 호출 또는 우선순위 sum(5, 3) : 5랑 3 먼저 가져오기 실행
    new 객체 생성 연산자 : 새로운 객체 생성
     */
}
